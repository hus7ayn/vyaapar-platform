# Ralph progress log — append-only learnings across iterations.
# Newest entries at the bottom. Keep entries short and factual.

## Codebase map (seeded from investigation)
- Monorepo: apps/api (NestJS 10 + Prisma/Supabase), apps/web (Next 15), packages/shared. pnpm workspace.
- Deploy: Vercel, two projects (vyaapar-api, vyaapar-web), rootDirectory set. DB schema managed by `prisma db push` (migrations were incomplete). Region bom1.
- Validate locally: `pnpm --filter @nexus/api typecheck` and `pnpm --filter @nexus/web lint`. API build: `pnpm --filter @nexus/api build` (swc). Rebuild shared after editing packages/shared: `pnpm --filter @nexus/shared build`.
- RBAC: roles in packages/shared/src/roles.ts, permission bundles in permissions.ts. Global guards: JwtAuthGuard + BranchScopeGuard (app.module). PermissionsGuard per-controller via @RequirePermissions. Branch/tenant isolation enforced server-side.

## Root causes found (before fixing)
- R3 party bug: create reads body.partyType (parties.service.ts:128, default CUSTOMER) but rest of app uses `type` (parties.controller.ts:22). Form sends `type` -> partyType undefined -> CUSTOMER. Fix: accept `type` alias + validate enum CUSTOMER|SUPPLIER|BOTH.
- R2 auto-refresh: use-pos-catalog.ts:73-76 has refetchOnMount:false, refetchOnWindowFocus:false, staleTime 10min, no poll; Pusher off (no keys) so nothing invalidates. Fix: enable refetch on mount/focus + lower staleTime.
- R4 password: backend already enforces >=8 + letter + digit (password-policy.ts). Eye toggle already exists (ui/password-input.tsx) on all auth forms. Only gap: staff-create form lacks client-side validation.
- R5 categories: item-category create needs client slug + no dup handling (categories.service.ts:16). No add-category UI for items/expenses (hotel room-types has one).
- R6 credit settle: only via POST /sale/payments-in (auto-allocates FIFO). No settle button on party/ledger UI.
- R9 return: refund API exists (sale.service.ts:112 refundInvoice, credit-notes, debit-notes). Missing UI button only.
- R7 payroll: generatePayroll scoped to one branch (payroll.service.ts:143 branchWhere). No advance/custom salary field.
- R8 RBAC: front-end pages not route-guarded (nav hidden only). Biller has no today's-sales view. super-admin portal exists at app/platform.
- R1 rename: APP_NAME in packages/shared/src/constants.ts + hardcoded literals (layout.tsx, sidebar.tsx, top-bar.tsx, platform-sidebar.tsx, pos/page.tsx) + "V" glyph + main.ts.

## Iterations

## Batch 2 (2026-07-24) — reported issues R11-R25. Validate switched to api+web typecheck
## (next lint prompts interactively + had 2 pre-existing errors in housekeeping). Added
## apps/web typecheck script + .eslintrc.json (next/core-web-vitals). Product decisions from
## user: signup CTA "Sign Up"; item required = Name+Category+SalePrice+CostPrice+Unit+OpeningStock+Barcode;
## party required = Name+Phone+Type; notifications = low-stock only.

- R11 (signup) PASS: CTA "Start Free Trial" -> "Sign Up" (signup/page.tsx:93). Added always-visible
  password-rule hint (PASSWORD_POLICY_MESSAGE, shown when no password error) under the password field so
  the existing zod+backend @IsStrongPassword enforcement is visible. Dropped "— free to get started" from
  subhead (trial framing). Confirmed: NO email verification anywhere (signup issues tokens immediately;
  no emailVerified field/guard) — nothing to remove. api+web typecheck green.

- R12 (dup-SKU error) PASS: added global PrismaExceptionFilter (common/filters/prisma-exception.filter.ts),
  registered via APP_FILTER in app.module.ts. Maps P2002 unique -> 409 with a field-aware message
  (sku -> "An item with this Item Code (SKU) already exists"; barcode/phone/email/gstin/name variants),
  P2025 -> 404, P2003 -> 409. Item create (unique [businessId,branchId,sku]) previously threw raw P2002 =>
  Nest default 500 "Internal server error"; now returns the friendly 409 the web client shows via toast.
  No per-service try/catch needed. api typecheck green.

- R13 (housekeeping header) PASS: root cause = /housekeeping and /services were top-level (app) routes,
  siblings of hotel/, so HotelLayout (header+tabs) never wrapped them; hotel tab linked to absolute
  /housekeeping -> leaving the group unmounted the header. Fix: git mv both pages into (app)/hotel/
  (hotel/housekeeping/page.tsx, hotel/services/page.tsx) so HotelLayout wraps them; updated hotel/layout.tsx
  Housekeeping href -> /hotel/housekeeping and added a Services tab (ConciergeBell); simplified sidebar
  Hotel-PMS match to p.startsWith('/hotel'). Backend API paths (/housekeeping, /services) unchanged (those
  are API calls). Changed both page roots h-[calc(100vh-4rem)] -> h-full to fit inside the hotel content
  area; fixed 2 react/no-unescaped-entities errors (task.notes quotes -> &ldquo;/&rdquo;). web typecheck +
  lint clean.

- R14 (add-category UI) PASS: replaced window.prompt with an inline in-app input in both places.
  items/page.tsx: addingCategory/newCategory state; "+ New" now reveals an inline text field + Add/✕
  (Enter=add, Esc=cancel) instead of prompt(). txn-form.tsx expense category: same pattern
  (addingExpenseCategory/newExpenseCategory). Both still POST /categories or /expenses/categories,
  invalidate, and auto-select the new category. web typecheck green.

- R15 (item required fields) PASS: per user, ADD ITEM now requires Name, Category, Sale Price, Cost Price,
  Unit, Opening Stock (no. of products), Barcode. Client: validateNewItem() in items/page.tsx (services skip
  openingStock+barcode); onSubmit runs it only for NEW items (edits stay name-only so legacy items still save);
  added conditional " *" markers on those labels (shown when !editingItem). Backend: items.service.create now
  takes opts.requireComplete and enforces the same set; controller POST /items passes requireComplete:true,
  while items.import calls create() WITHOUT it so partial bulk catalogs still load (see R17). api+web typecheck green.

- R16 (party required fields) PASS: ADD PARTY now requires Name + Phone + explicit Type. parties/page.tsx
  onSubmit validates phone + partyType; added " *" to Phone and Party Type labels. Backend:
  parties.service.create gained opts.requireComplete (phone + valid CUSTOMER|SUPPLIER|BOTH); controller
  POST /parties passes requireComplete:true; parties.import stays lenient. GOTCHA: POS customer-picker also
  POSTs /parties -> added client phone-required there too (placeholder "Phone *", submit guard) so the new
  backend rule never surprises; the "Walk-in customer" option creates NO party so anonymous sales still work.
  api+web typecheck green.

- R17 (import) PASS: client-only (utilities/page.tsx). Root cause was JSON-only import vs XLSX export +
  always-success toast hiding skipped/errors. Now: (1) CSV parser (RFC4180-ish: quotes, "" escape, CRLF),
  import accepts .csv AND .json, numeric columns coerced to Number per type (blank cells dropped);
  (2) real result panel showing created/skipped/failed + per-row error list from backend {created,skipped,errors};
  (3) "Download template (CSV)" per type (items/parties) with headers + example row; (4) Export gained an
  Excel/CSV format toggle (backend /exports already supported format=csv) so export->edit->import round-trips.
  Backend import already reports dup-SKU rows via its try/catch (surfaced now). web typecheck green.

- R18 (mobile switcher/menu) PASS: on mobile the sidebar (hidden lg:flex) — which holds ShopSwitcher + the
  "More" nav (Hotel PMS/Payroll/Staff/Settings) — was unreachable; bottom MobileNav had no switcher/hotel.
  Added stores/ui-store.ts (zustand: mobileNavOpen/toggle). top-bar.tsx: hamburger (Menu, lg:hidden) toggles it.
  sidebar.tsx: extracted its inner content into `body`, rendered in BOTH the desktop <aside> and a new
  lg:hidden fixed slide-in drawer (overlay + X close); useEffect closes the drawer on pathname/searchParams
  change (link tapped). Mobile users can now switch shops (ShopSwitcher) and open Hotel PMS from the drawer.
  web typecheck green.

- R19 (low-stock notifications) PASS: the bell had full plumbing but nothing ever created a Notification.
  Now TxnCoreService.applyStock creates a Notification when a stock-DECREASING txn crosses the item's minStock
  (or 0) downward — before>threshold && after<=threshold (single alert, no per-sale spam). type LOW_STOCK,
  userId null (business-wide so all users see it), metadata {itemId,currentStock,minStock}. Injected EventsGateway
  into TxnCoreService (imported EventsModule into the @Global TxnsModule) and call emitNotification for realtime;
  the row is created in-tx so the 60s bell poll surfaces it even with Pusher off. Uses existing Prisma.Decimal
  helpers (before.gt/after.lte, .toNumber()). Notifications = low-stock only per user's choice. api typecheck green.

- R20 (hotel categories) PASS: answers "where to add category" for hotels. New /hotel/categories page +
  "Categories" tab in hotel/layout.tsx. Reuses the retail Category backend scoped to the HOTEL branch. Backend:
  categories.controller findAll/create now accept an explicit branchId (query for GET, body for POST) that
  overrides the caller's active branch (needed because multi-branch admins view hotel via explicit branchId);
  added DELETE /categories/:id -> categories.service.remove (soft delete deletedAt). Page resolves hotel branch
  via /branches?type=HOTEL, lists/creates/deletes categories, and offers quick-add chips for Food/Beverages/
  Snacks/Desserts/Room Service. FOLLOW-UP (not done): wiring category filter into folio/services item picker +
  category reporting. api+web typecheck green.

- R21 (RBAC 3-tier clarity) PASS (scoped): settings/users staff-create now presents the three access tiers.
  Role <select> grouped via <optgroup> into "Manager — full access" (BRANCH_MANAGER/ACCOUNTANT),
  "Biller/Cashier — POS only" (BILLER), "Hotel staff" (RECEPTIONIST/HOUSEKEEPING/MAINTENANCE_STAFF); per-role
  scope description under the select; a tier explainer card (Super Admin/Manager/Biller) above the form; default
  role BILLER. The 3 tiers already exist as roles (SUPER_ADMIN/ADMIN+BRANCH_MANAGER/BILLER) with backend perms
  (R8) — this is the UI clarity Req#2 asked for. DEFERRED (documented): one Manager across MANY shops needs
  User.branchId (single) -> many-to-many + BranchScopeGuard + JWT changes, a large cross-cutting change unsafe
  for one ralph iteration. web typecheck green.

- R22 (credit payment form) PASS: replaced the window.prompt in parties/page.tsx collectPayment with a proper
  Dialog. openCollectPayment() opens it prefilled to full balance; submitPayment() validates (amount>0, not
  exceeding balance, bank account chosen when mode=BANK) and posts to /sale/payments-in or /purchase/payments-out
  with payments:[{paymentType:CASH|BANK, amount, bankAccountId?}] (TxnPaymentInput) + description:note
  (maps to Txn.description) + autoAllocate FIFO. Partial payments supported (toast notes "partial"). Added a
  bank-accounts query (/cash-bank/accounts). Per-invoice manual allocation left as FIFO (backend accepts explicit
  allocations if ever needed). web typecheck green.

- R23 (return/refund/exchange) PASS: was whole-invoice full refund only. Backend refundInvoice now takes
  {lineIds?, cashRefund?, mode?}: full (no lineIds) keeps the exact old path (bill discount/charges, cash=txn.total,
  invoice->REFUNDED); partial (lineIds) returns selected WHOLE lines with roundOffEnabled:false so cash refund =
  sum(stored line.total) matches createTxn's total EXACTLY (no rounding mismatch/throw). mode EXCHANGE (or
  cashRefund:false) records the return as store credit (no cash out) so a replacement can be sold. Inventory is
  restored via the CREDIT_NOTE txn engine (unchanged). Frontend: POS recent-orders-panel Return button now opens
  an item-picker dialog (fetches /txns/:id for lines, checkboxes default-all) with Refund (cash) and Exchange
  (closes to POS to ring up replacement) actions. Return HISTORY already exists as the "Sale Return / Credit Note"
  list (/sale/credit-notes). LIMITATION (documented): one return per invoice (guard on txn.returns); partial-QUANTITY
  within a line and bill-discount proration on partials deferred. api+web typecheck green.

- R24 (label designer) PASS: labels were hardcoded 50x25mm Name+price+barcode. New lib/label-config.ts
  (LabelConfig: field toggles name/category/size/colour/sku/mrp/price/barcodeNumber, widthMm/heightMm/marginMm/
  fontPt/barcodeHeightMm, align, paper presets) persisted in localStorage (msw-label-config) — no prisma migration.
  print-tags.ts rewritten to render from the config (dynamic @page size, fields in order, alignment, barcode
  height, optional barcode-number line); TagSpec extended with category/sku/mrp/size/colour. BarcodeTagPanel now
  passes category/sku/mrp from the selected item. Settings: new LabelDesignerCard (field chips, paper preset +
  W/H/margin/font/barcode-height/alignment, Save/Reset). Size/Colour render only if item has that data (base item
  model lacks them). Persistence is per-device (localStorage); syncing via FirmSettings is a follow-up. web typecheck green.

- R10 (barcode/thermal) PASS: barcode multi-print already existed on item detail (BarcodeTagPanel qty + Print Tags) — changed default qty 1->2 ("2 slips at once"). Thermal customization: added FirmSettings.receiptHeader/receiptFooter (schema; db push on deploy adds columns), settings.service accepts them, generateThermal loads firmSettings and injects header (under shop name) + footer (replaces "Thank you!"). Settings UI: new "Thermal Receipt" card with header/footer inputs. api + web typecheck green. ALL 10 STORIES PASS.

- R8 (RBAC) PASS (core): added Biller lockdown in (app)/layout.tsx — role BILLER is redirected to /pos from any non-/pos route (client guard; backend PermissionsGuard already protects data/APIs). Added "Today's Sales" badge on POS header (GET /sale/invoices?from=today&to=today summary.totalAmount, 60s refresh) — the one figure a Biller may see, no P&L. Super-admin portal + manager/owner branch scoping already enforced server-side (BranchScopeGuard). FOLLOW-UP (not done): assigning a Manager to MULTIPLE shops (user has single branchId today) + 3-tier relabel in staff UI. web typecheck green.

- R7 (payroll) PASS: generatePayroll now business-wide — fetches all active staff, groups by branch, creates one DRAFT payroll per shop (skips shops already generated), returns {created,skipped,payrolls}. Controller generate drops branchId. Added PayrollLine.advance (schema.prisma; prisma generate run; db push will add column on deploy) factored into calcNet (net = base+ot+bonus-deductions-advance). updateLine accepts advance. UI: generate toasts shop count; per-line Bonus + Advance now editable alongside Deductions (custom/advance salary). api + web typecheck green.

- R6 (credit settle) PASS: added "Collect Payment"/"Pay Now" button on party detail (parties/page.tsx collectPayment) shown when currentBalance != 0. Customer (bal>0) -> POST /sale/payments-in; supplier (bal<0) -> POST /purchase/payments-out; both autoAllocate:true FIFO against open invoices/bills (flips OPEN->PARTIAL/PAID). Amount via prompt, defaults to full balance. Frontend-only; reused existing endpoints.

- R9 (return) PASS: refund/return already wired in TWO places (txn-list-page sale invoices + POS recent-orders-panel), both POS_REFUND-gated. Real gap was discoverability: buttons were unlabeled ↻ icons. Added visible "Return" label to both + a confirm() to the POS panel. Backend refundInvoice unchanged.

- R5 (add-category) PASS: backend categories.service.create now auto-generates a unique slug from name (loop-dedup on businessId+branchId+slug) + validates name -> no more raw Prisma unique errors; controller slug optional. Frontend: inline "+ New" add-category on item form (items/page.tsx addCategory) and expense form (txn-form.tsx addExpenseCategory) via window.prompt -> POST -> invalidate -> auto-select. Hotel room categories already had an add flow. api typecheck green.

- R4 (password) PASS: eye toggle already present everywhere (PasswordInput). Added client-side validation to settings/users staff-create (isStrongPassword + required-field check via new submit()) plus a visible rule hint. isStrongPassword/PASSWORD_MIN_LENGTH exported from @nexus/shared (index re-exports password-policy). No shared rebuild needed.

- R3 (party supplier) PASS: backend verified CORRECT via live API (POST partyType=SUPPLIER persists+filters as SUPPLIER). Real cause was frontend: "Add Party" always reset to EMPTY_FORM (partyType CUSTOMER) even on the Suppliers tab. Fixed openCreate() in parties/page.tsx to default partyType to the active typeFilter. POS customer-picker intentionally hardcodes CUSTOMER (correct). No backend change.

- R2 (auto-refresh) PASS: use-pos-catalog.ts query now refetchOnMount:true, refetchOnWindowFocus:true, staleTime 30s, refetchInterval 60s (foreground). Global list queries already refetch on mount/focus (providers.tsx staleTime 60s). Pusher stays the instant path. No typecheck run (config-literal change).

- R1 (rename) PASS: APP_NAME='MSW Global' (constants.ts, shared rebuilt); literals in layout.tsx/top-bar/platform-sidebar/sidebar(nav 'MSW POS')/pos header; brand glyph "V"->"M" (sidebar, pos, login x2, signup); main.ts swagger+log. api typecheck green. Only code identifiers (VyaparActionButton, components/vyapar dir, --vyapar css vars, vyaapar-insights localStorage keys) remain — non-user-facing, left as-is.
